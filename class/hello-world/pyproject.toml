[project]
name = "hello-world"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests==2.31.0",
]

[tool.uv.sources]
requests = { git = "https://github.com/psf/requests" }
